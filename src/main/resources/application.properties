# PostgreSQL admin connection
db.admin.url=jdbc:postgresql://localhost:5432/postgres
db.admin.user=postgres
db.admin.password=postgres

# Teacher connection
db.teacher.user=teacher_role
db.teacher.password=teacher_pass

# Teacher secret for login
teacher.secret=teacher123

# Security limits
db.query.timeout=30
db.max.rows=1000