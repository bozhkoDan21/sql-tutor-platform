# SQL Trainer Platform

Учебная платформа для практического освоения SQL с возможностью загрузки тематических баз данных преподавателем, сессионной аутентификацией, визуализацией планов выполнения запросов и мониторингом активности студентов.

## 📋 Функциональность

### 🔐 Аутентификация и безопасность
- **Аутентификация преподавателя по паролю** — простая и безопасная, без хранения личных данных
- **HTTP-сессии** — управление состоянием через сессии браузера
- **Фильтры авторизации** (TeacherFilter для API, ViewResolver для JSP)
- **Хранение паролей баз данных** в зашифровантом виде (BCrypt)
- **Пароль преподавателя** только через переменные окружения (без дефолта в коде)
- **Защита от XSS** (CSP заголовки, экранирование вывода, X-XSS-Protection)
- **Защита от SQL-инъекций** (валидация идентификаторов, quote_ident, regexp)
- **Rate limiting** (семафор на 10 параллельных запросов, защита от DoS)
- **Таймауты запросов** (statement_timeout = 3 секунды для студентов)
- **Ограничение строк** (MAX_ROWS = 1000 для студентов)
- **Защита от опасных SQL команд** (блокировка DELETE, DROP, TRUNCATE, pg_sleep)
- **Валидация имён БД** (только буквы, цифры, подчёркивания)
- **Защищённые базы данных** (системные PostgreSQL нельзя удалить)
- **Проверка доступа студентов к БД** (белый список разрешённых баз + пароль + период доступа)
- **Изоляция соединений** (отдельные пулы для TEACHER и STUDENT)
- **Защитные HTTP-заголовки:**
    - Content-Security-Policy
    - X-Content-Type-Options: nosniff
    - X-Frame-Options: DENY
    - X-XSS-Protection: 1; mode=block
- **Логирование подозрительных действий** (попытки доступа к чужим БД, не-SELECT запросы)
- **Управление сессиями** — мониторинг активных сессий студентов с отображением:
    - IP-адреса
    - Последнего выполненного запроса
    - Времени выполнения
    - Времени последней активности
    - Статуса сессии (активна/заблокирована)
- **Блокировка/разблокировка сессий** — преподаватель может принудительно завершить или разблокировать сессию студента
- **Автоочистка заблокированных сессий** — удаление через 5 минут для оптимизации памяти

### 🗂️ Управление базами данных (преподаватель)

- **Папки (категории)** — группировка баз по группам, типам заданий или семестрам
- **Парольная защита** — каждая база может быть защищена паролем (BCrypt)
- **Видимость** — возможность скрыть базу от студентов
- **Период доступа** — задаётся дата начала и окончания доступа к базе
- **Отображаемое имя** — понятное название для студентов
- **Схема базы данных** — загрузка изображения со связями таблиц
- **Владелец базы** — только создатель может изменять/удалять базу

### 👨‍🏫 Для преподавателя

- **Полный SQL-доступ в тренажёре** — преподаватель может выполнять любые SQL-команды (CREATE, INSERT, UPDATE, DELETE, DROP, ALTER) через страницу тренажёра
- **Мониторинг сессий** — отслеживание активных сессий студентов в реальном времени
- **Блокировка/разблокировка сессии** — принудительное завершение или разблокировка сессии студента
- **Загрузка SQL-скриптов** для создания новых учебных баз данных:
    - Поддержка `.sql` файлов
    - Ограничение размера (2MB)
    - Защита от опасных SQL команд
    - Реальное время выполнения с **SSE-логами** (Server-Sent Events)
    - Прогресс-бар и детальный лог выполнения
- **Загрузка схемы БД** — прикрепление изображения схемы для студентов
- **Управление папками** — создание, просмотр папок для группировки баз
- **Управление базами данных** — просмотр, редактирование метаданных и удаление существующих БД
- **Управление доступом** — установка пароля, видимости и периода доступа
- **📚 Автоматическая генерация вопросов для Moodle** — экономия времени преподавателя:
    - Загрузка текстового файла в формате "вопрос → SQL запрос"
    - Автоматическое выполнение запросов к выбранной БД
    - Получение эталонных результатов
    - Экспорт в форматы:
        - **GIFT** (работает в любом Moodle из коробки) — рекомендуется
        - **Moodle XML** (требуется плагин CodeRunner)
        - **Текстовый просмотр** (для проверки)
    - При обновлении базы данных — достаточно перезапустить генерацию

### 🧑‍🎓 Для студентов

- **Выбор папки и базы данных** — иерархическая навигация
- **Ввод пароля** — защищённые паролем базы требуют аутентификации
- **Просмотр схемы БД** — визуализация структуры базы (если загружена)
- **Выполнение SELECT запросов** с ограничением по времени (3 сек) и максимальным количеством строк (1000)
- **Ограничения безопасности** — студенты НЕ могут выполнять INSERT, UPDATE, DELETE, DROP и другие изменяющие данные команды
- **Автодополнение SQL** (таблицы, колонки, ключевые слова)
- **Подсветка синтаксиса SQL** (CodeMirror)
- **Горячие клавиши** (`Ctrl+Enter` — выполнить, `Ctrl+Space` — автодополнение, `Ctrl+Shift+F` — формат)
- **Просмотр структуры базы** — список всех таблиц в выбранной БД
- **Кеширование результатов** (30 секунд, ускоряет повторные запросы)
- **Анализ плана выполнения** — визуализация `EXPLAIN ANALYZE`:
    - Древовидное представление плана с возможностью раскрытия/сворачивания узлов
    - Текстовый вывод PostgreSQL
    - Цветовое выделение типов узлов (Index Scan, Seq Scan, Join, Aggregate)
    - Отображение стоимости и времени выполнения
- **Экспорт результатов** — скачивание CSV с метаданными (время, количество строк, подпись сервера SHA-256)
- **Форматирование SQL** — автоматическое форматирование запросов
- **Подсказки при ошибках** — анализ ошибок и рекомендации по исправлению

## 🛠 Технологический стек

### Backend
- **Java 17** — основной язык
- **Apache Tomcat 9** — сервер приложений
- **Servlets / JSP** — обработка запросов и представления
- **JDBC** — доступ к данным
- **HikariCP** — пул соединений
- **BCrypt** — хеширование паролей баз данных
- **Gson** — работа с JSON
- **SLF4J + Logback** — логирование

### Frontend
- **HTML5 / CSS3** — вёрстка и стили
- **Vanilla JavaScript** — клиентская логика
- **CodeMirror** — редактор SQL с подсветкой
- **Server-Sent Events (SSE)** — потоковые логи при загрузке скриптов

### База данных
- **PostgreSQL 15** — основная СУБД
- **plpython3u** — генерация тестовых данных
- **pg_trgm** — полнотекстовый поиск

### Инфраструктура
- **Maven** — сборка проекта
- **Docker / Docker Compose** — контейнеризация
- **.env** — конфигурация через переменные окружения

### Тестирование
- **PowerShell / Bash скрипты** — тестирование безопасности и нагрузки
- **Тесты безопасности** (XSS, SQL injection, защита БД)

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- (Опционально) Maven для ручной сборки

### Запуск

```bash
# Клонировать репозиторий
git clone <repository-url>
cd sql-tutor-platform

# Скопировать и настроить .env
cp .env.example .env
# Отредактировать .env при необходимости

# Запустить развертывание
# Windows:
scripts\deploy\deploy.bat

# Linux:
./scripts/deploy/deploy.sh
```
### После развертывания
1. Дождитесь завершения сборки (первый запуск может занять 30-40 минут из-за генерации тестовых данных)

2. Проверьте статус контейнеров:
```bash
cd docker
docker-compose ps
```
Оба контейнера (```sql_trainer_postgres``` и ```sql_trainer_app```) должны быть в статусе Up
3. Откройте приложение в браузере:
   * Главная страница: ```http://localhost:8081```
   * Страница входа: ```http://localhost:8081/login```
   * Панель преподавателя: ```http://localhost:8081/teacher```

4. Войдите в систему:
   * Пароль: ```teacher123```

5. Для преподавателя:
   * Создайте папку для группировки баз
   * Загрузите SQL-скрипт для создания новой учебной базы
   * Настройте видимость, пароль и период доступа
   * Загрузите схему базы данных (изображение)
   * Отслеживайте активные сессии студентов
6. Для студента:
   * Выберите папку и базу данных
   * Введите пароль (если требуется)
   * Напишите SQL-запрос (только SELECT)
   * Нажмите ```Ctrl+Enter``` или кнопку "Выполнить"
   * При необходимости включите ```EXPLAIN``` для анализа плана запроса

### Проверка работоспособности
```bash
# Запустить быстрый тест безопасности
cd src/test/scripts/security
./test_security.sh   # Linux/Mac
test_security.ps1    # Windows

# Запустить нагрузочный тест
cd ../load
./load_test.sh       # Linux/Mac
load_test.ps1        # Windows
```
### Остановка приложения
```bash
cd docker
docker-compose down
```
### Полная переустановка (с удалением всех данных)
```bash
cd docker
docker-compose down -v
docker-compose up -d --build
```
### Структура проекта
```bash
SQLTrainer/
├── docker/                     # Docker файлы
│   ├── Dockerfile              # Tomcat образ
│   ├── Dockerfile.postgres     # PostgreSQL с расширениями
│   └── docker-compose.yml
├── scripts/
│   ├── db/                     # SQL скрипты
│   │   ├── setup_metadata.sql  # Таблицы метаданных (папки, сессии)
│   │   └── setup_database.sql  # Тестовые учебные БД (опционально)
│   └── deploy/                 # Скрипты развертывания
│       ├── deploy.bat          # Windows
│       └── deploy.sh           # Linux
├── src/
│   ├── main/
│   │   ├── java/com/sqltrainer/
│   │   │   ├── config/         # DatabaseConfig
│   │   │   ├── filter/         # EncodingFilter, TeacherFilter
│   │   │   ├── listener/       # AppContextListener
│   │   │   ├── servlet/        # LoginServlet, StudentServlet, TeacherServlet и др.
│   │   │   └── util/           # QueryExecutor, Constants, MoodleGenerator
│   │   ├── resources/
│   │   │   └── logback.xml
│   │   └── webapp/
│   │       ├── css/            # style.css
│   │       ├── js/             # script.js, teacher.js
│   │       ├── pages/          # JSP страницы
│   │       └── WEB-INF/        # web.xml
│   └── test/
│       └── scripts/            # Тестовые скрипты
│           ├── security/       # Тесты безопасности
│           └── load/           # Нагрузочные тесты
├── .env.example                # Шаблон конфигурации
├── pom.xml
└── README.md
```
### Переменные окружения
Все настройки задаются в файле ```.env ``` (скопируйте ```.env.example ``` и отредактируйте):
```bash
# База данных
DB_HOST=postgres
DB_PORT=5432
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=postgres
DB_TEACHER_USER=teacher_role
DB_TEACHER_PASSWORD=teacher_pass
DB_STUDENT_USER=students
DB_STUDENT_PASSWORD=student_pass

# Аутентификация преподавателя
TEACHER_PASSWORD=teacher123

# Ограничения запросов
QUERY_TIMEOUT_SEC=3
MAX_ROWS=1000
CONNECTION_TIMEOUT_MS=5000

# Ограничение параллельных запросов
MAX_CONCURRENT_QUERIES=10
SEMAPHORE_TIMEOUT_SEC=30

# Логирование
LOG_LEVEL=INFO
```

### Тестирование
В папке ```src/test/scripts/ ``` находятся скрипты для тестирования:

|**Скрипт**	|**Назначение**	|
|-----------|----------|
|```test_security.sh/ps1```|	Тесты безопасности (аутентификация, защита БД)	|
|```security/test_xss.sh/ps1```	|Тесты XSS-защиты	|
|```load/load_test.sh/ps1```|Нагрузочное тестирование (MAX_ROWS, таймауты, семафор)|

### 📝 Лицензия
Проект создан в рамках дипломной работы. Свободное использование в учебных целях.

### 👨‍💻 Автор
bozhkoDan21